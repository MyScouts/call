name: ui
description: A new Flutter package project.
version: 0.0.2
homepage: /
publish_to: "none" # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.17.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # features
  universal_platform: ^1.0.0+1
  intl: ^0.18.1

  # UI
  flutter_svg: ^2.0.8
  scrollable_positioned_list: ^0.3.8
  imagewidget: ^1.1.0
  app_core:
    path: ../app_core
  design_system:
    path: ../design_system
  dropdown_button2: ^2.3.9
  flutter_form_builder: ^9.1.1

  searchfield:
    git: https://github.com/FightTech-VN/searchfield.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
