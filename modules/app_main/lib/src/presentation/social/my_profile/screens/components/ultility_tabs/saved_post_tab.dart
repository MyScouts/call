import 'package:flutter/material.dart';

class SavedPostTab extends StatelessWidget {
  const SavedPostTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Mục đã lưu'));
  }
}