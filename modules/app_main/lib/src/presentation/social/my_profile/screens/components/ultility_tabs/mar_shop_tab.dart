import 'package:flutter/material.dart';

class MarShopTab extends StatelessWidget {
  const MarShopTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('MarShop'));
  }
}