import 'package:flutter/widgets.dart';

class GroupsEmptyWidget extends StatelessWidget {
  const GroupsEmptyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Empty Groups');
  }
}
